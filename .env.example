# Flask Secret Key
SECRET_KEY=change-this-to-a-random-secret-key

# Fernet Encryption Key (Generate using Fernet.generate_key().decode())
ENCRYPTION_KEY=

# Optional Mail Server Settings (for security alerts)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
